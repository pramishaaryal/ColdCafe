{
    "name": "cold-cafe-frontend",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "start": "vite preview --host 0.0.0.0 --port ${PORT:-10000}"
    },
    "devDependencies": {
        "vite": "^7.1.3"
    }
}